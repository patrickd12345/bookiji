'use client'

import HomePageWrapper from '../HomePageWrapper'

/**
 * Main Landing Page
 * 
 * This is the main landing page that was previously shown at the root.
 * Now accessible at /main
 */
export default function MainPage() {
  return <HomePageWrapper />
}
