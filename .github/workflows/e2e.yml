name: E2E
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

on:
  pull_request:
    branches: [ "**" ]
  workflow_dispatch: {}

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: preview
    permissions:
      contents: read
      actions: read
    concurrency:
      group: e2e-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Playwright browsers
        id: cache-playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright browsers
        if: steps.cache-playwright.outputs.cache-hit != 'true'
        run: pnpm dlx playwright install --with-deps

      - name: Prepare environment
        run: |
          echo "NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" >> $GITHUB_ENV
          echo "SUPABASE_SECRET_KEY=${{ secrets.SUPABASE_SECRET_KEY }}" >> $GITHUB_ENV
          echo "SENDGRID_API_KEY=${{ secrets.SENDGRID_API_KEY }}" >> $GITHUB_ENV
          echo "TWILIO_ACCOUNT_SID=${{ secrets.TWILIO_ACCOUNT_SID }}" >> $GITHUB_ENV
          echo "TWILIO_AUTH_TOKEN=${{ secrets.TWILIO_AUTH_TOKEN }}" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_ADSENSE_APPROVAL_MODE=true" >> $GITHUB_ENV
          echo "ADSENSE_APPROVAL_MODE=true" >> $GITHUB_ENV

      - name: Lint
        run: pnpm -s lint || true

      - name: Typecheck
        run: pnpm -s type-check || true

      - name: Forbid Vitest in E2E
        run: |
          # forbid vitest imports outside unit test dirs
          if rg -n "from 'vitest'|require\\('vitest'\\)" -- tests src | rg -v "src/.*/(__tests__|\\.test|\\.spec)\\."; then
            echo "❌ Found forbidden 'vitest' import outside unit tests."
            exit 1;
          fi
          echo "✅ No forbidden Vitest imports found"

      - name: Forbid Playwright in Non-Test Files
        run: |
          # forbid @playwright/test imports outside test dirs
          if rg -n "from '@playwright/test'|require\\(['\"]@playwright/test['\"]\\)" --glob '!tests/**' . ; then
            echo "❌ Found forbidden '@playwright/test' import outside tests/";
            exit 1;
          fi
          echo "✅ No forbidden Playwright imports found"

      - name: Run Playwright tests
        run: pnpm dlx @playwright/test test --reporter=html

      - name: Collect axe JSON
        if: always() && (github.ref == 'refs/heads/main' || failure())
        run: |
          mkdir -p axe-artifacts
          # Collect attachments wherever Playwright stored them
          find . -type f -path "**/test-results/**/attachments/axe-*.json" -exec cp {} axe-artifacts/ \; || true
          find . -type f -path "**/playwright-report/**/attachments/axe-*.json" -exec cp {} axe-artifacts/ \; || true

      - name: Upload axe artifacts
        if: always() && (github.ref == 'refs/heads/main' || failure())
        uses: actions/upload-artifact@v4
        with:
          name: axe-json-${{ github.run_number }}
          path: axe-artifacts
          if-no-files-found: ignore
          retention-days: 3

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 3


