#!/usr/bin/env sh
# Optimized pre-commit hook - only checks staged files for speed
# Full type-check and lint run in CI

echo "🔍 Running lint-staged (ESLint on staged files only)..."
pnpm lint-staged || {
  echo "❌ Lint-staged failed. Please fix errors before committing."
  exit 1
}

echo "✅ Pre-commit checks passed!"
