#!/usr/bin/env sh
echo "🔍 Running ESLint (errors only)..."
pnpm eslint . --quiet || {
  echo "❌ ESLint errors found. Please fix errors before committing."
  exit 1
}

echo "✅ ESLint passed. Running lint-staged..."
pnpm lint-staged
