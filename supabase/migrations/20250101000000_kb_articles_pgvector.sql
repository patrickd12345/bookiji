-- This migration now only adds sample data since the table structure is created in the base migration

-- Functions will be added in a separate migration if needed

-- Sample data will be inserted separately to avoid conflicts

-- RLS policies are now created in the base migration
